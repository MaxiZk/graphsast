{
  "name": "@graphsast/viz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run build -w @graphsast/core",
    "prepreview": "npm run build",
    "dev": "vite",
    "prebuild": "npm run build -w @graphsast/core",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@graphsast/core": "0.0.0",
    "cytoscape": "^3.30.4"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
