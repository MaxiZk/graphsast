{
  "name": "@graphsast/viz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run build -w @graphsast/core",
    "prepreview": "npm run build -w @graphsast/core && npm run build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@graphsast/core": "0.0.0",
    "cytoscape": "^3.30.4"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
